"""
ai_curate -- Shared AI curation backend for Image Curator.

Provides reusable modules for:
- Element extraction from prompts (elements)
- Configuration defaults (config)
- Run data models (models)
- Llama-swap-compatible vision client (client)
- Scoring orchestration (scoring)
- Run history storage (storage)
- Single-job queue management (queue)
- Web job submission validation (job_validation)
- Flask worker orchestration core (worker)
"""
