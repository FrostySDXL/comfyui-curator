# Convenience install file for pip install -r.
# Runtime constraints live in requirements.in; this file includes them directly.
# For pip-compile output (transitive deps with == pins), use requirements-lock.txt.
-r requirements.in
